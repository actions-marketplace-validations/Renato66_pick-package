name: 'Pick package'
description: 'Pick some scripts from package.json with a fixed version pre defined'
author: 'Renato66'

inputs:
  dependencies:
    required: true
    description: 'dependencies splitted by line break'
  clear-resolutions:
    description: 'return empty object for resolutions'
  clear-prepare:
    description: 'remove prepare from scripts'

runs:
  using: 'node16'
  main: 'dist/index.js'
branding:
  icon: 'align-left'
  color: 'green'
